# Mobius 3분 시연 시나리오

1. **문제(20초)** — 단일 게시물보다 계정·해시태그·반복 문구가 동시에 움직이는 초기 국면을 포착한다.
2. **로그인(20초)** — Google OAuth 또는 데모 세션으로 진입한다. 역할과 조직은 자체 DB의 초대·승인으로 결정된다.
3. **위험 근거(40초)** — 대시보드의 위험도, 유해성 모델, 멀티라벨 공격 유형, 권장 조치를 설명한다.
4. **그래프(40초)** — 계정·대상·해시태그 연결, 클러스터, 중심성, 동시성 점수를 보여준다. 협조 행동 점수는 확정 판정이 아니라 검토 신호임을 고지한다.
5. **멀티모달·증거(30초)** — 이미지 OCR과 유사 이미지 탐지, SHA-256 증거 ZIP을 시연한다.
6. **대응(20초)** — 이메일 경보, 쿨다운·묶음 정책, 조직별 이력 격리를 설명한다.
7. **한계(10초)** — synthetic demo·허가 데이터 범위, 오탐 가능성, 인간 검토 필요성을 명시한다.

## 시연 전 점검

- `docker compose up -d` 후 `http://localhost:8000/health`가 `ok`인지 확인
- 프론트는 `npm.cmd run dev:web`로 실행
- `.env`의 Google/SMTP 값은 화면이나 발표 자료에 노출하지 않기
